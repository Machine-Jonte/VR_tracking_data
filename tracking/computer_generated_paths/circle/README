The folder names are the time it takes for the circle to do a full loop.
The circle is calculated as:
	y = cos(2 * pi * 1.0/self.T * t)
        z = sin(2 * pi *  1.0/self.T * t)
where T is the folder name.
